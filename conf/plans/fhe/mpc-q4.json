{
  "rels": [
    {
      "id": "0",
      "relOp": "LogicalValues",
      "outputFields": "o_orderpriority (#0), o_orderdate (#1)",
      "type": [
        {"type": "CHAR", "precision": 15, "name": "o_orderpriority"},
        {"type": "LONG", "nullable": false, "name": "o_orderdate"}
      ],
      "sql": "SELECT o_orderpriority, o_orderdate FROM orders o JOIN lineitem l ON l.l_orderkey = o.o_orderkey WHERE l_commitdate < l_receiptdate ORDER BY o_orderpriority",
      "collation": [
        {"field": 0, "direction": "ASCENDING"}
      ],
      "tuples": [],
      "inputs": []
    },
    {
      "id": "1",
      "relOp": "LogicalFilter",
      "condition": {
        "op": {"name": "AND", "kind": "AND", "syntax": "BINARY"},
        "operands": [
          {
            "op": {"name": ">=", "kind": "GREATER_THAN_OR_EQUAL", "syntax": "BINARY"},
            "operands": [
              {"input": 1, "name": "$1"},
              {"literal": 741484800, "type": {"type": "LONG", "nullable": false}}
            ]
          },
          {
            "op": {"name": "<", "kind": "LESS_THAN", "syntax": "BINARY"},
            "operands": [
              {"input": 1, "name": "$1"},
              {"literal": 749433600, "type": {"type": "LONG", "nullable": false}}
            ]
          }
        ]
      }
    },
    {
      "id": "2",
      "relOp": "LogicalAggregate",
      "operator-algorithm": "sort-merge-aggregate",
      "cardBound": 5,
      "group": [0],
      "effective-collation": [
        {"field": 0, "direction": "ASCENDING", "nulls": "FIRST"}
      ],
      "aggs": [
        {
          "agg": {"name": "COUNT", "kind": "COUNT", "syntax": "FUNCTION_STAR"},
          "type": {"type": "BIGINT", "nullable": false},
          "distinct": false, "operands": [], "name": "order_count"
        }
      ]
    }
  ]
}
